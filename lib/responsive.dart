//import 'package:flutter/material.dart' ;

//const double webMobileBreakPoint = 875;
//const double mobileTabletBreakPoint = 600;
//class ResponsiveWidget extends StatelessWidget{
  // final Widget web;
   //final Widget mobile;
   //final Widget? tablet;
   //const ResponsiveWidget({
      //super.key,

      //required this.web,
     // this.tablet,
     // required this.mobile,
//});
   //@override
   //Widget build(BuildContext context) {
      //if (Responsive.isWeb(context)) {
        // return web;
     // } else if (Responsive.isTablet(context)) {
     //    return tablet ?? mobile;
     // } else
       //  return mobile;
  // }
  // }


//class Responsive{
 //  static  bool isWeb(BuildContext context)=> MediaQuery.sizeOf(context).width>= webMobileBreakPoint;
 //  static  bool isTablet(BuildContext context)=> MediaQuery.sizeOf(context).width>= mobileTabletBreakPoint &&
     //  MediaQuery.sizeOf(context).width < webMobileBreakPoint;


   //static  bool isMobile(BuildContext context)=> MediaQuery.sizeOf(context).width < mobileTabletBreakPoint;
//}